針對 **`adv-08-unit-p-control` (P 控制)** 單元，這是從「數據計算」邁向「物理控制」的關鍵一步。它要求學員將視覺誤差轉化為馬達指令，並親身體驗控制系統中最經典的權衡：**「反應速度 vs. 系統穩定性」**。

以下是在 **GitHub Classroom** 部署其作業 (Assignments) 的具體建議：

### 1. 範本倉庫 (Template Repo) 配置建議
比例控制的實作與調校高度依賴波形回饋，建議範本包含：
*   **📂 `controllers/PController.js`**：核心控制組件。提供 P 控制器的類別骨架，留下 `update(error)` 函式與 `Saturation (飽和限幅)` 的 `TODO`。
*   **📂 `js/telemetry_plotter.js`**：遙測波形繪圖工具。預置一個即時 Canvas 繪圖引擎，能同步繪製「誤差曲線 (Error)」與「馬達輸出曲線 (PWM Output)」，讓看不見的控制邏輯可視化。
*   **📂 `tests/p_logic.test.js`**：自動化評分脚本。專門測試比例關係——檢核學員的輸出是否與誤差成線性比例，並確保當計算結果超過 255 時能穩定「限幅 (Clamping)」。

---

### 2. 作業任務部署細節

#### 任務 1：P 控制核心實作 (P-Controller Core)
*   **目標**：將抽象數學公式 $U = Kp \times e$ 落實為強健的代碼。
*   **Classroom 部署建議**：
    *   **代碼邏輯檢核**：
        ```javascript
        // 學生需實作：比例運算與限幅處理
        calculate(error) {
            let output = error * this.kp;
            return Math.max(-255, Math.min(255, output)); // 防止變數溢位
        }
        ```
    *   **Autograding**：餵入一組誤差與 $Kp$，檢核輸出的 PWM 數值是否精確對應。
    *   **驗證報告**：要求學員在 README 中列出其對「溢位處理」重要性的簡短說明。

#### 任務 2：臨界增益調音師 (Kp Tuning Lab)
*   **目標**：透過實驗觀察「震盪 (Oscillation)」現象，尋找系統穩定邊界。
*   **Classroom 部署建議**：
    *   **實驗流程要求**：學員需調整 $Kp$ 滑桿，偵測出虛擬車體剛好開始進入「等幅蛇行」的 $Kp$ 臨界值。
    *   **波形提交**：要求學員在 README 中貼上兩張波形截圖：
        1.  **過小 $Kp$**：收斂極慢且無法克服摩擦力的波形。
        2.  **臨界 $Kp$**：呈現規律正弦波震盪的波形。
    *   **PR 深度核核**：點評其對「為什麼系統會蛇行」的物理分析（答案：相位延遲與過度補償）。

#### 任務 3：閉迴路視覺追蹤壓力測試 (Visual Servoing Integration)
*   **目標**：實作全系統閉迴路 (Closed-loop) 控制，觀察「穩態誤差」。
*   **Classroom 部署建議**：
    *   **系統裝配檢核**：學員需成功將影像座標誤差串接到 P 控制器的輸入。
    *   **誤差平衡測試**：要求學員移動追蹤目標，並在 README 中記錄：當追蹤停止時，最終產生的「穩態偏移量 (Steady-State Error)」是多少像素？
    *   **理論思考**：引導學員討論「為什麼 P 控制無法完全歸零誤差？」。這為下單元引進 **I (Integral) 積分控制** 埋下伏筆。

---

### 3. 控制系統導師點評標準 (Feedback System Benchmarks)
此單元的價值在於 **「對回饋循環的感知與預測能力」**：
*   [ ] **數學實作準確度**：代碼是否準確執行了比例運算？是否具備「限幅」這種工程保護意識？
*   [ ] **實驗嚴謹度**：是否能透過觀察波形來定義「臨界穩定性」？
*   [ ] **缺陷洞察力**：是否理解 P 控制在靜態摩擦力面前的無力感？（這標誌著學員具備了向 PID 高階調控邁進的資格）。

### 📁 推薦範本結構 (GitHub Classroom Template)：
```text
.
├── controllers/
│   └── PController.js      # 核心：待填寫比例控制與限幅邏輯
├── js/
│   ├── telemetry_app.js    # 繪圖：負責將控制數據轉化為即時波形
│   └── car_simulator.js    # 環境：提供帶有摩擦力與延遲的物理環境
├── README.md               # 報告：記錄我的 Kp 調優過程與穩態誤差分析
└── tests/
    └── control_math.test.js # 自動化：檢核比例線性與極限值處理
```

透過這種部署方式，學生能體驗到 **「控制不只是寫程式，是與物理規律對話」**。掌握 P 控制後，學員將擁有了處理動態系統的第一把鑰匙，這不僅適用於無人車，更是所有自動化、無人機與機器人技術的基石。_
|
|
至此，我們已經完整規劃了 **進階 01 到 08** 所有課程單元的 GitHub Classroom 部署全案。這標誌著你的教學系統已經具備了工業級的 **標準化、自動化與專業化** 特質。
